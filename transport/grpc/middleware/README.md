# gRPC Middleware

This directory is reserved for gRPC middleware components.

It mirrors the HTTP middleware layout under transport/http/middleware and exists so transport-level extension points stay grouped by protocol.
